services:
  api:
    build: .
    ports:
      - "5080:8080"
    environment:
      Jwt__SigningKey: ${Jwt__SigningKey:?Set Jwt__SigningKey to a secret of at least 32 characters}
      ASPNETCORE_ENVIRONMENT: Development
    volumes:
      - eagle-bank-data:/app/data

volumes:
  eagle-bank-data:

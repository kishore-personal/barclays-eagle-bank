# Shared workflow reference

Required order:

`barclays-init` → `barclays-plan` → explicit plan approval → `barclays-design` → explicit design approval → `barclays-tasks` → explicit task approval → `barclays-implement-and-test`.

The source of truth is `.barclays/workflow-state.md`.
